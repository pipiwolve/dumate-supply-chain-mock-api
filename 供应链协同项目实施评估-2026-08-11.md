# 供应链/采购协同：DuMate 项目实施评估报告

> **项目概述**：为外部客户提供一套 DuMate 供应链协同解决方案，覆盖供应商沟通归档、大促备货方案生成、补货分析报告、异常订单预警四大核心场景。
> **评估日期**：2026-08-11

---

## 一、需求框架回顾

### 1.1 需求痛点（客户侧现状）

| 编号 | 痛点场景 | 核心问题 | 改善目标 |
|------|----------|----------|----------|
| 01 | **供应商沟通** | 多家供应商沟通协调，邮件/微信信息分散；跟进状态靠人记 | 跟进状态需要自动化记录 |
| 02 | **大促备货方案** | 大促备货需要跨销售预测/库存/供应商多方数据整合；多系统手工取数、汇总耗时出错 | 备货方案数据急需整合 |
| 03 | **补货需求分析** | 补货需求分析依赖经验；货物库存/销售数据在多个系统中分散 | 补货分析报告急需标准化 |

### 1.2 DuMate 解决方案框架

| 模块 | 能力描述 | 关键动作 |
|------|----------|----------|
| **供应商沟通记录结构化归档** | 与供应商的邮件、聊天记录和会议纪要统一入库；关键节点（交期确认/到货状态/异常反馈）自动提取并生成跟进清单；采购人员每日查看清单即可掌控全部供应商状态，无需依赖个人记忆或手工台账 | 文本解析 + 结构化存储 + 状态追踪 |
| **促销备货方案自动生成** | 上传销售预测文件、历史库存数据和本次促销计划；DuMate 自动关联计算各 SKU 备货建议量，生成分品类、分供应商的采购需求清单；采购直接在初稿上审核调整，消除从多系统手工取数拼表的过程 | 数据整合 + 公式计算 + 方案生成 |
| **补货分析报告标准化输出** | 周期性将库存数据和销售数据导入 DuMate，自动计算安全库存缺口、滞销品预警和高周转品补货优先级；生成标准化补货分析报告，替代人工逐 SKU 分析 | 数据导入 + 自动计算 + 报告生成 |
| **异常订单即时预警** | 采购订单状态变更（延期/缺货/品质异常）触发自动通知；相关人员即时获知，而非等到交付节点才发现问题 | 状态监控 + 阈值判断 + 即时通知 |

---

## 二、实施路径与技术方案

### 2.1 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    供应链协同解决方案                         │
│                          (DuMate)                            │
├─────────────────────────────────────────────────────────────┤
│  模块1        模块2          模块3          模块4          │
│  供应商沟通   促销备货方案   补货分析报告    异常订单预警      │
│  归档        自动生成       标准化输出      即时通知         │
├─────────────────────────────────────────────────────────────┤
│                     数据接入层 (MCP Server)                   │
│  ┌──────────┬──────────┬──────────┬──────────┐             │
│  │ ERP/OMS  │  WMS     │  CRM     │ 邮件/聊天 │             │
│  │ 库存系统  │ 仓储系统  │ 供应商   │ 平台      │             │
│  └──────────┴──────────┴──────────┴──────────┘             │
├─────────────────────────────────────────────────────────────┤
│                     展示与通知层                             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │ 实时监控看板  │    │ 定时报告      │    │ 即时预警      │  │
│  │ (HTML/大屏)  │    │ (每周一/周期) │    │ (条件触发)    │  │
│  └──────────────┘    └──────────────┘    └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 Phase 1：供应链系统 MCP 接入（核心前置）

#### 2.2.1 数据源详细说明

供应链协同方案需要接入以下 4 类核心数据源，每类数据源的字段、格式和接入方式如下：

**数据源 A：ERP/OMS 订单管理系统**

| 数据表/接口 | 字段清单 | 数据格式 | 更新频率 | 接入方式 |
|-------------|----------|----------|----------|----------|
| 采购订单表 (purchase_orders) | 订单号、供应商ID、SKU、数量、单价、订单日期、要求交期、实际交期、订单状态（待确认/已确认/生产中/已发货/已入库/已取消）、付款方式、账期 | JSON/CSV | 实时 | API 查询或数据库只读视图 |
| 采购订单明细表 (purchase_order_items) | 订单号、SKU、批次号、数量、单价、折扣、税率、实际到货数量、质检结果 | JSON/CSV | 实时 | API 查询或数据库只读视图 |
| 到货记录表 (receiving_records) | 到货单号、订单号、SKU、到货数量、质检合格数量、质检不合格数量、到货日期、入库仓库 | JSON/CSV | 实时 | API 查询或数据库只读视图 |

**数据源 B：WMS 仓储管理系统**

| 数据表/接口 | 字段清单 | 数据格式 | 更新频率 | 接入方式 |
|-------------|----------|----------|----------|----------|
| 库存实时表 (inventory_snapshot) | SKU、仓库ID、库位、可用库存、在途库存、预留库存、冻结库存、安全库存阈值、最后更新时间 | JSON/CSV | 实时（分钟级） | API 查询或数据库只读视图 |
| 库存流水表 (inventory_transactions) | 流水ID、SKU、仓库ID、变动类型（入库/出库/盘点/调拨/报损）、变动数量、关联单据号、操作时间、操作人 | JSON/CSV | 实时 | API 查询或数据库只读视图 |
| 仓库主数据表 (warehouses) | 仓库ID、仓库名称、仓库类型（自有仓/第三方仓/海外仓）、地址、联系人、状态 | JSON/CSV | 低频 | 文件同步或 API |

**数据源 C：CRM/供应商管理系统**

| 数据表/接口 | 字段清单 | 数据格式 | 更新频率 | 接入方式 |
|-------------|----------|----------|----------|----------|
| 供应商主数据表 (suppliers) | 供应商ID、供应商名称、统一社会信用代码、联系人、联系电话、邮箱、地址、合作状态（活跃/暂停/终止）、评级（A/B/C/D）、账期（天）、付款方式 | JSON/CSV | 低频 | API 查询或数据库只读视图 |
| 供应商沟通记录表 (supplier_communications) | 记录ID、供应商ID、沟通类型（邮件/电话/微信/会议）、沟通内容、沟通时间、我方联系人、供应商联系人、关键节点标记（交期确认/到货状态/异常反馈/价格谈判）、待办事项、完成状态 | JSON/CSV | 实时 | API 查询或文件导入 |
| SKU 主数据表 (sku_master) | SKU、SKU名称、品类、品牌、规格、单位、采购 lead time（天）、安全库存天数、供应商ID、最小采购量、标准采购价 | JSON/CSV | 低频 | API 查询或文件同步 |

**数据源 D：销售预测与计划数据**

| 数据表/接口 | 字段清单 | 数据格式 | 更新频率 | 接入方式 |
|-------------|----------|----------|----------|----------|
| 销售预测表 (sales_forecast) | SKU、预测日期、预测销量、预测金额、预测来源（系统预测/人工调整/大促计划）、置信度 | JSON/CSV/Excel | 每日/每周 | API 查询或文件导入 |
| 大促计划表 (promotion_plan) | 活动ID、活动名称、开始日期、结束日期、参与SKU、预期销量增幅、备货系数、优先级 | JSON/CSV/Excel | 按需 | 文件导入或 API |
| 历史销售表 (sales_history) | SKU、销售日期、销售渠道、销售数量、销售金额、退货数量 | JSON/CSV | 每日 | API 查询或数据库只读视图 |

#### 2.2.2 MCP Server 详细设计

**技术架构**

采用 MCP 2026-07-28 无状态核心规范，开发 1 个供应链 MCP Server：

- **开发语言**：Python 3.10+
- **MCP SDK**：`mcp` Python SDK（`pip install mcp`）
- **传输方式**：HTTP/SSE（无状态，支持远程部署和横向扩展）
- **数据格式**：JSON（输入输出）
- **认证机制**：OAuth 2.0 Client Credentials 或 API Key + IP 白名单
- **部署方式**：Docker 容器化部署，支持本地/云服务器/K8s

**MCP Server 工具集详细定义**

工具 1：`get_inventory` — 查询实时库存

```json
{
  "name": "get_inventory",
  "description": "查询指定 SKU 在指定仓库的实时库存快照，包括可用库存、在途库存、预留库存和安全库存阈值。支持按 SKU、仓库、品类等多维度筛选。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sku_id": {
        "type": "string",
        "description": "SKU 编码，支持模糊匹配（如 'SKU-2024-*'）"
      },
      "warehouse_id": {
        "type": "string",
        "description": "仓库 ID，不传则查询所有仓库"
      },
      "category": {
        "type": "string",
        "description": "品类名称，用于批量查询某一品类库存"
      },
      "include_safety_stock": {
        "type": "boolean",
        "default": true,
        "description": "是否返回安全库存阈值"
      }
    }
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "sku_id": "string",
        "sku_name": "string",
        "warehouse_id": "string",
        "warehouse_name": "string",
        "available_stock": "number",
        "in_transit_stock": "number",
        "reserved_stock": "number",
        "frozen_stock": "number",
        "safety_stock_threshold": "number",
        "stock_status": "string (正常/低于安全库存/缺货)",
        "last_updated": "string (ISO 8601)"
      }
    }
  }
}
```

工具 2：`get_sales_forecast` — 获取销售预测

```json
{
  "name": "get_sales_forecast",
  "description": "获取指定时间段和 SKU 的销售预测数据，支持按日/周/月聚合。预测数据可能来自系统算法预测、人工调整或大促计划。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sku_id": {
        "type": "string",
        "description": "SKU 编码，支持多个 SKU 用逗号分隔"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期，格式 YYYY-MM-DD"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期，格式 YYYY-MM-DD"
      },
      "aggregation": {
        "type": "string",
        "enum": ["daily", "weekly", "monthly"],
        "default": "daily",
        "description": "聚合粒度"
      },
      "include_promotion": {
        "type": "boolean",
        "default": true,
        "description": "是否包含大促计划中的预测增量"
      }
    },
    "required": ["start_date", "end_date"]
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "sku_id": "string",
        "forecast_date": "string",
        "forecast_quantity": "number",
        "forecast_amount": "number",
        "forecast_source": "string (系统预测/人工调整/大促计划)",
        "confidence": "number (0-1)",
        "promotion_flag": "boolean"
      }
    }
  }
}
```

工具 3：`get_purchase_orders` — 查询采购订单

```json
{
  "name": "get_purchase_orders",
  "description": "查询采购订单列表及详情，支持按供应商、订单状态、日期范围、SKU 等多维度筛选。返回订单基本信息和明细行项目。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "supplier_id": {
        "type": "string",
        "description": "供应商 ID"
      },
      "status": {
        "type": "string",
        "enum": ["pending", "confirmed", "in_production", "shipped", "received", "cancelled", "all"],
        "default": "all",
        "description": "订单状态"
      },
      "date_range": {
        "type": "object",
        "properties": {
          "start": "string (YYYY-MM-DD)",
          "end": "string (YYYY-MM-DD)"
        }
      },
      "sku_id": {
        "type": "string",
        "description": "SKU 编码，筛选包含该 SKU 的订单"
      },
      "is_delayed": {
        "type": "boolean",
        "description": "是否只查询延期订单（要求交期已过但未入库）"
      }
    }
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "order_id": "string",
        "supplier_id": "string",
        "supplier_name": "string",
        "order_date": "string",
        "required_delivery_date": "string",
        "actual_delivery_date": "string (nullable)",
        "status": "string",
        "total_amount": "number",
        "payment_terms": "string",
        "items": [
          {
            "sku_id": "string",
            "quantity": "number",
            "unit_price": "number",
            "received_quantity": "number",
            "qc_passed": "number",
            "qc_failed": "number"
          }
        ]
      }
    }
  }
}
```

工具 4：`get_supplier_info` — 查询供应商信息

```json
{
  "name": "get_supplier_info",
  "description": "查询供应商主数据和近期合作概况，包括基本信息、联系人、评级、账期以及最近 90 天的订单执行情况。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "supplier_id": {
        "type": "string",
        "description": "供应商 ID，不传则返回所有活跃供应商"
      },
      "include_performance": {
        "type": "boolean",
        "default": true,
        "description": "是否包含近 90 天订单执行统计"
      }
    }
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "supplier_id": "string",
        "supplier_name": "string",
        "contact_person": "string",
        "contact_phone": "string",
        "contact_email": "string",
        "address": "string",
        "status": "string (活跃/暂停/终止)",
        "rating": "string (A/B/C/D)",
        "payment_terms_days": "number",
        "payment_method": "string",
        "performance_summary": {
          "total_orders_90d": "number",
          "on_time_delivery_rate": "number (0-1)",
          "avg_lead_time_days": "number",
          "qc_pass_rate": "number (0-1)"
        }
      }
    }
  }
}
```

工具 5：`get_order_status` — 查询订单状态变更

```json
{
  "name": "get_order_status",
  "description": "查询采购订单的状态变更历史或最新状态，支持批量查询。可用于检测订单是否延期、是否发生异常状态变更。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "订单号，支持多个用逗号分隔"
      },
      "since_timestamp": {
        "type": "string",
        "description": "查询该时间之后的状态变更，格式 ISO 8601"
      },
      "include_history": {
        "type": "boolean",
        "default": false,
        "description": "是否返回完整状态变更历史，false 则只返回最新状态"
      }
    }
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "order_id": "string",
        "current_status": "string",
        "status_history": [
          {
            "status": "string",
            "changed_at": "string",
            "changed_by": "string",
            "remark": "string"
          }
        ],
        "delay_flag": "boolean",
        "delay_days": "number",
        "abnormal_flag": "boolean",
        "abnormal_type": "string (延期/缺货/品质异常/其他)"
      }
    }
  }
}
```

工具 6：`upload_communication` — 上传沟通记录

```json
{
  "name": "upload_communication",
  "description": "将采购人员与供应商的沟通记录（邮件、微信、会议纪要等）上传到 DuMate 进行结构化归档。系统自动提取关键节点（交期确认、到货状态、异常反馈、价格谈判）并生成跟进清单。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "supplier_id": {
        "type": "string",
        "description": "供应商 ID"
      },
      "communication_type": {
        "type": "string",
        "enum": ["email", "phone", "wechat", "meeting", "other"],
        "description": "沟通类型"
      },
      "content": {
        "type": "string",
        "description": "沟通内容原文，支持长文本"
      },
      "communication_time": {
        "type": "string",
        "description": "沟通发生时间，格式 ISO 8601"
      },
      "our_contact": {
        "type": "string",
        "description": "我方联系人"
      },
      "supplier_contact": {
        "type": "string",
        "description": "供应商联系人"
      },
      "attachments": {
        "type": "array",
        "items": {
          "filename": "string",
          "content_base64": "string"
        },
        "description": "附件列表（可选）"
      }
    },
    "required": ["supplier_id", "communication_type", "content", "communication_time"]
  },
  "returns": {
    "type": "object",
    "properties": {
      "archive_id": "string",
      "status": "string (success/failed)",
      "extracted_key_points": [
        {
          "type": "string (交期确认/到货状态/异常反馈/价格谈判/其他)",
          "description": "string",
          "action_item": "string (nullable)"
        }
      ],
      "follow_up_tasks": [
        {
          "task_id": "string",
          "task_description": "string",
          "due_date": "string (nullable)",
          "status": "string (待办/已完成)"
        }
      ]
    }
  }
}
```

工具 7：`calculate_replenishment` — 计算补货建议

```json
{
  "name": "calculate_replenishment",
  "description": "基于当前库存、销售预测、采购 lead time 和安全库存阈值，自动计算各 SKU 的补货建议量和优先级。支持按品类、供应商或仓库维度聚合。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sku_id": {
        "type": "string",
        "description": "SKU 编码，支持多个用逗号分隔，不传则计算所有 SKU"
      },
      "category": {
        "type": "string",
        "description": "品类名称，用于批量计算"
      },
      "forecast_period_days": {
        "type": "integer",
        "default": 30,
        "description": "预测覆盖天数"
      },
      "buffer_multiplier": {
        "type": "number",
        "default": 1.0,
        "description": "缓冲系数（大促期间可调高，如 1.5）"
      }
    }
  },
  "returns": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "sku_id": "string",
        "sku_name": "string",
        "current_stock": "number",
        "safety_stock": "number",
        "forecast_demand_30d": "number",
        "suggested_replenishment_qty": "number",
        "priority": "string (高/中/低)",
        "priority_reason": "string",
        "suggested_supplier_id": "string",
        "suggested_supplier_name": "string",
        "estimated_lead_time_days": "number",
        "estimated_arrival_date": "string"
      }
    }
  }
}
```

#### 2.2.3 数据映射与转换逻辑

MCP Server 需要处理客户系统数据格式到标准格式的映射：

**映射配置示例（以采购订单状态为例）**

```yaml
# config/data_mapping.yaml
purchase_order_status:
  source_field: "order_status"
  mappings:
    "待确认": "pending"
    "已确认": "confirmed"
    "生产中": "in_production"
    "已发货": "shipped"
    "已入库": "received"
    "已取消": "cancelled"
    "Partial": "partial_received"

date_formats:
  source: "YYYY/MM/DD HH:mm:ss"
  target: "ISO8601"

sku_id_format:
  source_prefix: "PROD-"
  target_prefix: ""
  normalization: true
```

**数据转换流水线**

```
客户系统原始数据
    ↓
[数据提取层] — 通过 API / 数据库 / 文件读取原始数据
    ↓
[数据清洗层] — 处理空值、去重、格式标准化（日期/金额/编码）
    ↓
[数据映射层] — 字段映射、状态码转换、单位统一
    ↓
[数据验证层] — JSON Schema 校验、业务规则校验（如交期必须大于订单日期）
    ↓
[标准数据输出] — 返回给 DuMate 的统一格式 JSON
```

#### 2.2.4 认证与安全机制

**推荐方案：OAuth 2.0 Client Credentials + API Key 双因素**

```
┌─────────────┐         ┌─────────────────┐         ┌─────────────┐
│  DuMate     │ ───1──→ │  Auth Server    │ ←──── │  Client     │
│  (Client)   │ 请求Token│ (OAuth2/IAM)    │ 颁发   │  Credentials│
└─────────────┘         └─────────────────┘         └─────────────┘
       │
       │ 2. 携带 Access Token + API Key
       ↓
┌─────────────┐
│ MCP Server  │ ←── 验证 Token 有效性 + API Key 白名单
│ (Resource)  │ ←── 读取请求来源 IP（白名单校验）
└─────────────┘
       │
       │ 3. 携带 Service Token
       ↓
┌─────────────┐
│ 客户 ERP/   │
│ WMS/CRM     │
└─────────────┘
```

**安全要求清单**

| 安全项 | 实现方式 | 说明 |
|--------|----------|------|
| 传输加密 | TLS 1.3 | 所有 HTTP/SSE 通信强制 HTTPS |
| 身份认证 | OAuth 2.0 + API Key | 双因素认证，Token 有效期 1 小时 |
| 访问控制 | RBAC | 区分只读权限和读写权限 |
| 请求限流 | Rate Limiting | 每分钟最多 1000 次请求 |
| 数据脱敏 | 字段级加密 | 供应商联系人电话/邮箱加密存储 |
| 审计日志 | 全量记录 | 记录每次数据查询的请求人、时间、数据范围 |
| IP 白名单 | 网络层控制 | 仅允许 DuMate 服务器 IP 访问 |

#### 2.2.5 MCP Server 部署架构

```
┌─────────────────────────────────────┐
│           Docker Compose            │
│  ┌─────────────┐  ┌─────────────┐  │
│  │ MCP Server  │  │ Redis Cache │  │
│  │  (Python)   │  │  (数据缓存)  │  │
│  │  Port: 8080 │  │  Port: 6379 │  │
│  └─────────────┘  └─────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  │
│  │ Nginx       │  │ Log Collector│  │
│  │ (反向代理)   │  │ (审计日志)   │  │
│  │ Port: 443   │  │ Port: 9200  │  │
│  └─────────────┘  └─────────────┘  │
└─────────────────────────────────────┘
              │
              │ HTTPS (TLS 1.3)
              ↓
┌─────────────────────────────────────┐
│           客户数据中心              │
│  ┌─────────┐ ┌─────────┐ ┌────────┐│
│  │   ERP   │ │   WMS   │ │  CRM   ││
│  │  API    │ │  API    │ │  API   ││
│  └─────────┘ └─────────┘ └────────┘│
└─────────────────────────────────────┘
```

**容器化部署配置示例**

```yaml
# docker-compose.yml
version: '3.8'
services:
  mcp-supply-chain:
    image: supply-chain-mcp:v1.0
    ports:
      - "8080:8080"
    environment:
      - MCP_TRANSPORT=http
      - AUTH_MODE=oauth2+apikey
      - OAUTH_SERVER_URL=https://auth.client.com
      - API_KEY=${SUPPLY_CHAIN_API_KEY}
      - ERP_API_URL=https://erp.client.com/api/v1
      - WMS_API_URL=https://wms.client.com/api/v1
      - CRM_API_URL=https://crm.client.com/api/v1
      - REDIS_URL=redis://redis:6379
      - LOG_LEVEL=info
    depends_on:
      - redis
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    restart: unless-stopped
  
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - mcp-supply-chain

volumes:
  redis_data:
```

#### 2.2.6 MCP Server 实施详细步骤

**步骤 1：环境准备与依赖安装**

```bash
# 1.1 创建项目目录
mkdir supply-chain-mcp && cd supply-chain-mcp

# 1.2 初始化 Python 项目
python -m venv venv
source venv/bin/activate
pip install mcp fastapi uvicorn redis python-dotenv pydantic requests

# 1.3 创建项目结构
mkdir -p src/tools src/adapters src/mapping src/utils config logs tests
```

**步骤 2：配置数据源连接**

```bash
# 2.1 创建配置文件
cat > config/datasources.yaml << 'EOF'
erp:
  type: api
  base_url: https://erp.client.com/api/v1
  auth_type: oauth2
  client_id: ${ERP_CLIENT_ID}
  client_secret: ${ERP_CLIENT_SECRET}
  timeout: 30
  retry_times: 3

wms:
  type: api
  base_url: https://wms.client.com/api/v1
  auth_type: api_key
  api_key_header: X-API-Key
  api_key: ${WMS_API_KEY}
  timeout: 30

crm:
  type: database
  db_type: mysql
  host: ${CRM_DB_HOST}
  port: 3306
  database: crm_prod
  username: ${CRM_DB_USER}
  password: ${CRM_DB_PASS}
  read_only: true
EOF

# 2.2 配置数据映射
cat > config/mappings.yaml << 'EOF'
# 见上文数据映射与转换逻辑章节
EOF
```

**步骤 3：实现数据源适配器**

为每个客户系统开发适配器，统一封装数据读取逻辑：

```python
# src/adapters/erp_adapter.py
class ERPAdapter:
    """ERP 系统数据适配器"""
    
    def __init__(self, config):
        self.base_url = config['base_url']
        self.session = self._create_session(config)
    
    def get_purchase_orders(self, **filters):
        """获取采购订单"""
        endpoint = f"{self.base_url}/purchase_orders"
        response = self.session.get(endpoint, params=filters)
        return self._transform_orders(response.json())
    
    def get_receiving_records(self, **filters):
        """获取到货记录"""
        endpoint = f"{self.base_url}/receiving_records"
        response = self.session.get(endpoint, params=filters)
        return self._transform_receiving(response.json())
    
    def _transform_orders(self, raw_data):
        """将 ERP 原始数据转换为标准格式"""
        # 应用数据映射规则
        pass
```

**步骤 4：实现 MCP 工具**

```python
# src/tools/inventory_tool.py
from mcp import Tool

class GetInventoryTool(Tool):
    """查询实时库存工具"""
    
    name = "get_inventory"
    description = "查询指定 SKU 在指定仓库的实时库存快照..."
    input_schema = {
        "type": "object",
        "properties": {
            "sku_id": {"type": "string"},
            "warehouse_id": {"type": "string"},
            # ... 完整 schema
        }
    }
    
    def execute(self, params):
        wms_adapter = self.get_adapter('wms')
        inventory = wms_adapter.query_inventory(**params)
        
        # 补充安全库存阈值（从 CRM/SKU 主数据获取）
        sku_master = self.get_adapter('crm').get_sku_master(params.get('sku_id'))
        
        # 计算库存状态
        for item in inventory:
            item['stock_status'] = self._calculate_stock_status(
                item['available_stock'], 
                sku_master['safety_stock_threshold']
            )
        
        return inventory
```

**步骤 5：组装 MCP Server**

```python
# src/server.py
from mcp import MCPServer
from tools.inventory_tool import GetInventoryTool
from tools.sales_forecast_tool import GetSalesForecastTool
from tools.purchase_order_tool import GetPurchaseOrdersTool
from tools.supplier_tool import GetSupplierInfoTool
from tools.order_status_tool import GetOrderStatusTool
from tools.communication_tool import UploadCommunicationTool
from tools.replenishment_tool import CalculateReplenishmentTool

server = MCPServer("supply-chain-mcp")

# 注册所有工具
server.register_tool(GetInventoryTool())
server.register_tool(GetSalesForecastTool())
server.register_tool(GetPurchaseOrdersTool())
server.register_tool(GetSupplierInfoTool())
server.register_tool(GetOrderStatusTool())
server.register_tool(UploadCommunicationTool())
server.register_tool(CalculateReplenishmentTool())

# 启动 HTTP/SSE 传输
if __name__ == "__main__":
    server.run_http(host="0.0.0.0", port=8080)
```

**步骤 6：测试与验证**

```bash
# 6.1 单元测试
pytest tests/test_inventory_tool.py -v
pytest tests/test_purchase_order_tool.py -v

# 6.2 集成测试（连接客户测试环境）
python scripts/integration_test.py --env=staging

# 6.3 MCP 协议合规性测试
mcp-cli test --server http://localhost:8080

# 6.4 负载测试
locust -f tests/load_test.py --host=http://localhost:8080
```

**步骤 7：部署上线**

```bash
# 7.1 构建 Docker 镜像
docker build -t supply-chain-mcp:v1.0 .

# 7.2 启动服务
docker-compose up -d

# 7.3 健康检查
curl -H "Authorization: Bearer ${TOKEN}" \
     -H "X-API-Key: ${API_KEY}" \
     https://mcp.client.com/health

# 7.4 在 DuMate 中注册 MCP Server
# 通过 DuMate 设置界面添加 MCP Server 地址和认证信息
```

---

### 2.3 Phase 2：实时监控与分析看板

#### 2.3.1 看板展示维度

| 面板区域 | 指标/内容 | 数据来源 | 刷新频率 |
|----------|----------|----------|----------|
| **库存概览** | 总 SKU 数、缺货 SKU 数、滞销 SKU 数、安全库存达标率 | WMS/ERP | 实时/每小时 |
| **订单追踪** | 在途采购订单数、延期订单数、本周到货预测 | OMS/ERP | 实时/每小时 |
| **供应商状态** | 活跃供应商数、待回复沟通数、逾期跟进数 | CRM/沟通记录 | 每 2 小时 |
| **预警看板** | 缺货预警、延期预警、品质异常预警、库存积压预警 | 多系统聚合 | 实时 |
| **大促备货** | 当前备货完成率、缺口 SKU 列表、预计到货时间 | 预测+库存 | 每日 |

#### 2.3.2 实现方式

看板采用 **DuMate Custom Dashboard** 方式实现：

1. 使用 `eop-scene-custom-dashboard` skill 或 `frontend-design` skill 生成 HTML 看板
2. 看板通过 MCP Server 实时拉取供应链数据
3. 看板部署为可分享的 URL（本地预览或线上部署）
4. 支持自动刷新（轮询机制，每 15-30 分钟）

---

### 2.4 Phase 3：风险预警与定时报告

#### 2.4.1 异常预警规则引擎

| 预警类型 | 触发条件 | 通知方式 | 响应时效 |
|----------|----------|----------|----------|
| **缺货预警** | 库存 < 安全库存阈值 | 即时消息/邮件 | 实时 |
| **延期预警** | 采购订单交期已过且未入库 | 即时消息/邮件 | 每日扫描 |
| **品质异常** | 到货检验不合格率 > 5% | 即时消息/邮件 | 实时 |
| **库存积压** | 滞销 SKU 库存 > 90 天销量 | 周报/即时 | 每周 |
| **供应商沟通超时** | 待回复沟通 > 24 小时 | 每日提醒 | 每日 |

#### 2.4.2 定时报告机制

**每周一 09:00 自动生成并发送《供应链周报》**：

使用 `scheduler_create_job` 创建定时任务：

- **Cron 表达式**：`0 9 * * 1`（每周一上午 9:00）
- **执行内容**：
  1. 拉取上周库存变动、订单执行、供应商沟通数据
  2. 生成补货分析报告（含安全库存缺口、滞销预警、高周转补货优先级）
  3. 生成异常订单汇总（延期/缺货/品质异常清单）
  4. 输出为 Markdown/HTML 报告并发送邮件/飞书/如流

#### 2.4.3 报告模板结构

```
# 供应链周报 (YYYY-MM-DD)

## 一、核心指标速览
- 总 SKU 数 / 缺货 SKU 数 / 滞销 SKU 数
- 在途订单数 / 延期订单数
- 本周到货准确率

## 二、补货需求分析
### 2.1 安全库存缺口
| SKU | 当前库存 | 安全库存 | 缺口 | 建议补货量 | 优先级 |

### 2.2 滞销品预警
| SKU | 库龄 | 90天销量 | 建议处理方案 |

### 2.3 高周转补货优先级
| SKU | 周转天数 | 建议补货量 | 预计到货时间 |

## 三、异常订单跟进
| 订单号 | 供应商 | 异常类型 | 异常描述 | 当前状态 | 负责人 |

## 四、供应商沟通摘要
| 供应商 | 待办事项 | 最后沟通时间 | 待回复事项 |

## 五、本周行动计划
- [ ] 补货订单确认
- [ ] 延期订单跟进
- [ ] 滞销品处理
```

---

## 三、实施里程碑（细化版）

### 里程碑 1：数据接入验证（Week 1-2）

**目标**：打通客户供应链数据源，MCP Server 核心工具上线。

**Week 1：需求调研与技术设计**

- [ ] **Day 1-2：客户系统调研**
  - [ ] 与客户 IT 团队召开技术对接会
  - [ ] 获取 ERP/WMS/CRM 系统接口文档或数据库 Schema
  - [ ] 确认数据更新频率、数据量级、历史数据保留策略
  - [ ] 确认认证方式（OAuth2 / API Key / 数据库账号）
  - [ ] 输出《数据源调研报告》

- [ ] **Day 3-4：MCP Server 技术设计**
  - [ ] 设计 MCP Server 架构（传输方式、认证机制、部署方案）
  - [ ] 定义 7 个工具的详细 JSON Schema
  - [ ] 设计数据映射规则（字段映射、状态码转换、日期格式统一）
  - [ ] 设计缓存策略（Redis 缓存哪些数据、缓存有效期）
  - [ ] 输出《MCP Server 技术设计文档》

- [ ] **Day 5：环境搭建**
  - [ ] 搭建开发环境（Python 虚拟环境、Docker、Redis）
  - [ ] 创建项目目录结构和基础代码框架
  - [ ] 配置开发/测试/生产环境变量
  - [ ] 搭建代码仓库（Git）并配置 CI/CD 流水线（GitHub Actions）

**Week 2：MCP Server 开发与联调**

- [ ] **Day 1-2：适配器开发**
  - [ ] 开发 ERP 适配器（采购订单、到货记录接口封装）
  - [ ] 开发 WMS 适配器（库存查询接口封装）
  - [ ] 开发 CRM 适配器（供应商信息、SKU 主数据接口封装）
  - [ ] 实现数据清洗和映射逻辑

- [ ] **Day 3：核心工具开发**
  - [ ] 实现 `get_inventory` 工具（含库存状态计算逻辑）
  - [ ] 实现 `get_purchase_orders` 工具（含延期订单检测逻辑）
  - [ ] 实现 `get_supplier_info` 工具（含近 90 天绩效统计）

- [ ] **Day 4：高级工具开发**
  - [ ] 实现 `get_sales_forecast` 工具（支持大促计划叠加）
  - [ ] 实现 `calculate_replenishment` 工具（含补货优先级算法）
  - [ ] 实现 `upload_communication` 工具（含关键节点自动提取）

- [ ] **Day 5：联调测试**
  - [ ] 连接客户测试环境进行端到端测试
  - [ ] 验证每个工具的数据准确性和响应性能
  - [ ] 完成《MCP Server 联调测试报告》
  - [ ] 修复发现的问题并重新测试

**里程碑 1 验收标准**：
- [ ] MCP Server 7 个工具全部通过单元测试
- [ ] 连接客户测试环境成功获取数据
- [ ] 平均响应时间 < 2 秒（简单查询）
- [ ] 数据准确性验证通过（抽查 20 条记录与源系统对比）

---

### 里程碑 2：看板上线（Week 3-4）

**目标**：实时监控看板开发完成并交付用户使用。

**Week 3：看板开发**

- [ ] **Day 1：需求确认与原型设计**
  - [ ] 与采购团队确认看板面板布局和内容
  - [ ] 设计看板 UI 原型（Figma / 手绘）
  - [ ] 确认每个面板的数据源和刷新频率
  - [ ] 确认看板配色方案和视觉风格

- [ ] **Day 2-3：看板前端开发**
  - [ ] 搭建看板项目框架（HTML + CSS + JavaScript）
  - [ ] 开发库存概览面板（数字卡片 + 趋势图表）
  - [ ] 开发订单追踪面板（表格 + 状态标签 + 延期高亮）
  - [ ] 开发供应商状态面板（列表 + 待办计数）

- [ ] **Day 4-5：高级面板与交互**
  - [ ] 开发预警看板面板（颜色分级：红/黄/绿）
  - [ ] 开发大促备货面板（进度条 + 缺口清单）
  - [ ] 实现数据绑定逻辑（通过 MCP Server 拉取数据）
  - [ ] 实现自动刷新机制（轮询，15 分钟间隔）

**Week 4：看板测试与交付**

- [ ] **Day 1：看板测试**
  - [ ] 功能测试：各面板数据展示正确
  - [ ] 性能测试：页面加载时间 < 3 秒
  - [ ] 兼容性测试：Chrome / Edge / Safari
  - [ ] 用户试用：邀请 2-3 名采购人员试用

- [ ] **Day 2：问题修复与优化**
  - [ ] 收集用户反馈
  - [ ] 修复发现的问题
  - [ ] 优化数据加载速度（增加缓存、减少请求量）

- [ ] **Day 3-4：看板部署**
  - [ ] 确定部署方式（本地服务器 / 云服务器 / 内网部署）
  - [ ] 配置域名和 HTTPS 证书
  - [ ] 配置访问权限（IP 白名单 / 登录认证）
  - [ ] 完成《看板部署文档》

- [ ] **Day 5：培训与交付**
  - [ ] 编写《看板用户操作手册》
  - [ ] 对采购团队进行看板使用培训（30 分钟）
  - [ ] 正式交付看板访问链接
  - [ ] 收集首批使用反馈

**里程碑 2 验收标准**：
- [ ] 看板 5 个面板全部正常展示数据
- [ ] 数据自动刷新正常（15 分钟间隔）
- [ ] 页面加载时间 < 3 秒
- [ ] 至少 2 名采购人员确认看板满足日常使用需求

---

### 里程碑 3：预警与报告（Week 5-6）

**目标**：预警规则和定时报告机制上线运行。

**Week 5：预警规则开发**

- [ ] **Day 1：预警规则设计**
  - [ ] 与客户确认预警规则清单和阈值
  - [ ] 设计预警通知模板（邮件 / 飞书 / 如流消息格式）
  - [ ] 确认预警接收人列表和通知渠道
  - [ ] 输出《预警规则配置清单》

- [ ] **Day 2-3：核心预警规则实现**
  - [ ] 实现缺货预警规则（库存 < 安全库存阈值时触发）
  - [ ] 实现延期预警规则（交期已过未入库时触发）
  - [ ] 实现供应商沟通超时预警（待回复 > 24 小时触发）
  - [ ] 配置预警状态恢复逻辑（异常解除后发送恢复通知）

- [ ] **Day 4-5：预警通知集成**
  - [ ] 集成邮件通知渠道（SMTP 配置）
  - [ ] 集成飞书/如流通知渠道（Webhook / Bot API）
  - [ ] 实现预警去重机制（同一异常 4 小时内不重复通知）
  - [ ] 实现预警升级机制（未处理预警自动升级给上级）

**Week 6：定时报告与验收**

- [ ] **Day 1-2：定时报告模板开发**
  - [ ] 设计《供应链周报》Markdown/HTML 模板
  - [ ] 实现数据自动填充逻辑（库存、订单、补货、异常）
  - [ ] 实现图表生成（库存趋势图、订单执行率图）
  - [ ] 实现报告导出（PDF / HTML / Markdown）

- [ ] **Day 3：定时任务配置**
  - [ ] 使用 `scheduler_create_job` 创建定时任务
  - [ ] 配置 Cron 表达式：`0 9 * * 1`（每周一 09:00）
  - [ ] 配置任务执行监控和失败告警
  - [ ] 测试手动触发报告生成

- [ ] **Day 4：端到端测试**
  - [ ] 模拟缺货场景，验证预警通知及时送达
  - [ ] 模拟延期订单，验证预警内容准确
  - [ ] 手动触发周报生成，验证报告内容完整
  - [ ] 测试报告发送渠道（邮件 / 飞书 / 如流）

- [ ] **Day 5：项目验收与交付**
  - [ ] 编写《项目验收报告》（功能清单、测试用例、验收结论）
  - [ ] 整理全部交付物（代码、文档、配置文件）
  - [ ] 进行项目演示（向客户展示完整功能）
  - [ ] 客户签署验收确认书

**里程碑 3 验收标准**：
- [ ] 缺货预警和延期预警在模拟测试中正确触发
- [ ] 预警通知在 5 分钟内送达接收人
- [ ] 定时周报每周一 09:00 自动生成并发送
- [ ] 报告内容完整（含库存、订单、补货、异常四个维度）

---

## 四、项目风险提示

### 4.1 技术实施风险

| 风险项 | 影响程度 | 发生概率 | 缓解措施 |
|--------|----------|----------|----------|
| 客户供应链系统无标准 API | 高 | 中 | 提前确认接口文档；无 API 时采用 CSV/Excel 导入降级方案；预留适配器开发时间 |
| 数据权限审批延迟 | 中 | 高 | 尽早发起数据权限申请；准备脱敏测试数据先行开发；与客户 IT 建立直接沟通渠道 |
| MCP 协议版本兼容问题 | 低 | 低 | 使用 2026-07-28 最新稳定版本；做好版本锁定；关注官方升级公告 |
| 客户系统数据结构变更 | 中 | 中 | 数据映射配置化（YAML 配置而非硬编码）；建立变更通知机制；预留维护窗口 |
| 看板实时性要求过高 | 中 | 中 | 明确刷新频率需求（建议 15-30 分钟轮询）；高实时场景采用 WebSocket 推送升级方案 |

### 4.2 数据质量风险

| 风险项 | 影响程度 | 发生概率 | 缓解措施 |
|--------|----------|----------|----------|
| 源系统数据不完整 | 高 | 中 | 数据质量评估先行；缺失字段采用默认值或标记为"数据缺失"；推动客户完善数据录入 |
| 多系统数据不一致 | 高 | 高 | 建立数据校验规则（如订单总量 = 各仓库库存变动之和）；定期对账机制 |
| 历史数据缺失 | 中 | 中 | 明确历史数据可用范围；预测和趋势分析基于可用数据；与客户协商数据补录 |
| 编码格式不统一 | 低 | 中 | 数据清洗层统一处理（UTF-8 标准化）；SKU/供应商编码映射表 |

### 4.3 项目推进风险

| 风险项 | 影响程度 | 发生概率 | 缓解措施 |
|--------|----------|----------|----------|
| 客户配合度不足 | 高 | 中 | 签署配合承诺书；明确客户方对接人和响应时效；建立周例会机制 |
| 需求范围蔓延 | 中 | 高 | 签署范围确认书；变更需走正式变更流程；明确一期/二期/三期边界 |
| 用户接受度低 | 中 | 中 | 早期用户参与设计；试用反馈快速响应；提供充分培训材料和现场培训 |
| 通知渠道不可用 | 低 | 低 | 提前测试邮件/飞书/如流连通性；准备备用通知渠道；本地日志兜底 |

---

## 五、交付物清单

| 序号 | 交付物 | 类型 | 说明 |
|------|--------|------|------|
| 1 | 供应链 MCP Server | Python 项目 | 含 7 个核心工具，支持 HTTP/SSE 传输，Docker 容器化部署 |
| 2 | MCP Server 部署文档 | Markdown | 安装、配置、运行指南，含 Docker Compose 配置 |
| 3 | MCP Server API 文档 | Markdown | 7 个工具的详细 JSON Schema、请求/响应示例 |
| 4 | 数据映射配置 | YAML | 客户系统字段到标准格式的映射规则 |
| 5 | 实时监控看板 | HTML 页面 | 5 个面板，支持自动刷新，响应式布局 |
| 6 | 看板部署说明 | Markdown | 本地/线上部署步骤，含 Nginx 配置示例 |
| 7 | 预警规则配置 | YAML | 阈值、触发条件、通知模板、升级策略 |
| 8 | 定时周报模板 | Markdown/HTML | 可自动生成并发送，含图表 |
| 9 | 用户操作手册 | Markdown | 看板使用、预警解读、报告查看、常见问题 |
| 10 | 项目验收报告 | Markdown | 功能清单、测试用例、验收结论 |

---

## 五、后续迭代建议

在基础版本交付后，可按优先级迭代以下能力：

1. **预警规则扩展**：增加品质异常预警、供应商评分下降预警
2. **预测模型接入**：引入 AI 销量预测，替代人工预测输入
3. **供应商协同门户**：为供应商提供只读看板，共享交期/库存信息
4. **移动端适配**：开发移动端看板，支持随时随地查看
5. **历史数据归档**：建立数据仓库，支持跨年趋势分析

---

## 六、结论与建议

### 可行性结论

**项目整体可行，建议分三期交付**：

- **一期（必做）**：MCP Server 数据接入 + 实时监控看板 — 这是核心价值交付
- **二期（建议）**：2-3 个核心预警规则（缺货 + 延期 + 沟通超时）+ 定时周报
- **三期（可选）**：预警规则扩展 + 供应商协同门户 + 移动端适配

### 下一步行动建议

1. **立即确认客户供应链系统接口情况**（是否有 REST API / 数据库只读权限 / 文件导出能力）
2. **确认看板部署方式**（本地访问即可 / 需要公网可访问链接）
3. **确认通知渠道**（邮件 / 飞书 / 如流 / 其他）
4. **签署范围确认书**：明确本期交付的 3 个核心模块（供应商沟通归档、备货方案生成、补货报告）+ 2 个预警规则

---

> **备注**：本评估基于 DuMate 2026-08 可用技能生态（含 MCP 协议 2026-07-28 无状态版本）编制。实际实施时需根据客户具体系统环境进行调整。