# Dumate 供应链协同看板

## Direction

Operate：面向采购与计划团队的日常工作台。信息密度服务扫描、比较和处理，不使用营销式英雄区。

## Visual World

以“调度台 + 数据流转单”为视觉隐喻：深海军蓝承载导航和结论，冷白工作区承载表格，青绿表示可推进状态，琥珀/红色只用于风险。圆角保持克制，分隔线与留白建立层级。

## Layout

- 左侧固定导航，桌面端宽 240px；移动端变为顶部横向滚动导航。
- 首屏由风险摘要、补货优先级列表和异常订单组成，默认不要求用户先读说明。
- 数据表格优先于重复卡片；详情在同一页面按模块切换。

## Type And Color

- 字体：系统中文字体栈，数字使用等宽回退以便对齐。
- 颜色：`#102A56`、`#F6F8FB`、`#11A8A5`、`#E9A23B`、`#D94C5C`、`#2F6FED`。
- 状态使用文字 + 色点，不只依赖颜色传达含义。

## Interaction

- 导航切换、关键词筛选、状态筛选和刷新均为真实可用交互。
- 加载失败时显示明确错误与重试按钮；空结果时保留筛选条件并给出清除入口。
- 模拟数据标签始终可见，避免误读为客户真实数据。

