# Dumate 上游系统接入门户

## Direction

Operate：面向 Dumate 集成与测试人员的系统核验入口。信息密度服务确认连接、数据范围和接口，不提供业务分析看板。

## Visual World

以“系统联络单 + 数据目录”为视觉隐喻：深海军蓝承载服务身份，冷白工作区承载目录，青绿表示可用连接，琥珀只用于加载状态。圆角保持克制，分隔线与留白建立层级。

## Layout

- 左侧固定服务导航，桌面端宽 240px；移动端变为顶部横向滚动导航。
- 首屏展示服务身份、健康状态和三套系统目录，默认不要求用户先读说明。
- 数据表格和接口列表优先于重复卡片；MCP 地址与工具清单保持可复制、可核验。

## Type And Color

- 字体：系统中文字体栈，数字使用等宽回退以便对齐。
- 颜色：`#102A56`、`#F6F8FB`、`#11A8A5`、`#E9A23B`、`#D94C5C`、`#2F6FED`。
- 状态使用文字 + 色点，不只依赖颜色传达含义。

## Interaction

- 导航锚点、状态刷新和 MCP 地址复制均为真实可用交互。
- 加载失败时显示明确错误与重试按钮。
- 模拟数据标签始终可见，避免误读为客户真实数据。
