# Dumate 供应链案例演示 Runbook

这份 Runbook 用于在 MCP 已连接后，逐步搭建 Dumate 内的供应链协同演示。所有数据来自固定种子模拟器，生成时间为 `2026-08-11T09:00:00+08:00`，不得描述为真实客户数据或生产数据。

## 演示目标

用一条完整故事线证明 Dumate 能够：

1. 从 ERP/OMS、WMS、CRM 读取并关联事实数据；
2. 生成库存和订单风险分析；
3. 形成促销备货/补货建议草稿；
4. 把供应商沟通转成结构化跟进事项；
5. 在 Dumate 内生成看板、预警和定时周报。

模拟器只读，不创建订单、不写回数据、不发送外部消息。看板、预警、报告、任务和通知由 Dumate 自己完成。

## 0. 总控 Prompt

先将下面 Prompt 粘贴到 Dumate，用于建立本案例的工作上下文：

```text
你现在负责演示“供应链协同”案例。已连接 MCP：供应链上游系统模拟器。

数据源：ERP/OMS、WMS、CRM/供应商；数据标签：演示数据 · 固定种子；数据生成时间：2026-08-11 09:00（Asia/Shanghai）。
可用 MCP 工具只有：get_inventory、get_sales_forecast、get_purchase_orders、get_supplier_info、get_supplier_communications、get_order_status。

请遵守以下边界：
1. 只读取和分析上游数据，不写回 MCP，不创建采购订单；
2. 补货建议、预警、看板、报告、任务和通知由你在 Dumate 内完成；
3. 不得声称已经发送邮件/飞书/如流消息；任何通知先生成草稿并等待确认；
4. 所有结论标注“演示数据”，空结果或缺失字段标记为“数据不足”；
5. 每次输出包含：数据范围、调用工具、关键证据、结论、待确认项、行动草稿和权限边界。

请先不要生成看板，先执行连接验收并报告结果。
```

## 1. MCP 连接验收

```text
请执行供应链上游系统连接验收：
1. 确认 initialize 成功；
2. 调用 tools/list，确认返回 6 个工具；
3. 调用 get_inventory，参数 category=个护清洁；
4. 调用 get_purchase_orders，参数 is_delayed=true；
5. 从延期订单中选择 PO-260802-002，调用 get_order_status，参数 order_id=PO-260802-002、include_history=true；
6. 调用 get_supplier_info，参数 supplier_id=SUP-002；
7. 输出《MCP 连接验收记录》：每个调用的记录数、关键字段、异常、缺失字段和下一步建议。
```

预期演示结果：个护库存 2 条；延期订单 1 条（`PO-260802-002`）；供应商 `SUP-002` 可返回；工具总数 6 个。

## 2. 供应链运营总览

```text
请基于上游模拟数据生成“供应链运营总览”草稿，不做业务判断扩展：
1. 查询全部库存、全部采购订单和全部活跃供应商；
2. 统计 SKU 总数、低于安全库存 SKU 数、在途未完成订单数、延期订单数、活跃供应商数；
3. 列出库存状态最需要关注的前 5 个 SKU，展示可用、在途、预留、安全库存和仓库；
4. 列出所有延期/缺货订单，关联供应商、负责人、要求交期和延误天数；
5. 输出适合做 Dumate 数据看板的模块结构，但不要把页面写入上游系统。
```

建议 Dumate 看板模块：库存概览、订单状态、供应商状态、风险清单、待跟进事项。页面标题必须标注“演示数据”。

## 3. 异常订单预警

```text
请执行一次“异常订单扫描”，并在 Dumate 内生成预警草稿：
1. 调用 get_purchase_orders，分别获取 is_delayed=true 和全部未完成订单；
2. 对 PO-260802-002 通过 get_order_status 获取完整状态历史；
3. 对 PO-260805-005 通过 get_order_status 获取最新状态和历史；
4. 按以下规则分级：缺货且可能影响活动为 P0；已延期为 P1；沟通待回复超过 24 小时为 P2；一般状态变化为 P3；
5. 每条预警输出订单号、供应商、SKU（若明细可关联）、异常类型、证据、负责人、建议动作、截止时间；
6. 生成通知草稿，但不要发送。
```

演示重点：`PO-260802-002` 展示延期预警，`PO-260805-005` 展示缺货预警。预警状态、通知记录和处理按钮由 Dumate 保存，不要要求 MCP 写入。

## 4. 夏日生活节促销备货

```text
请搭建“夏日生活节”促销备货分析：
活动上下文：PROMO-2608-001，活动日期 2026-08-18 至 2026-08-24，参与 SKU 为 SKU-1001 至 SKU-1008，优先级高，备货系数 1.35。

请执行：
1. 对 SKU-1001 至 SKU-1008 查询库存；
2. 查询这些 SKU 的销售预测；当前模拟预测日为 2026-08-18；
3. 查询全部未完成采购订单，并关联订单明细中的 SKU；
4. 查询相关供应商信息；
5. 对每个 SKU 计算：可覆盖库存 = 可用 + 在途 - 预留；预测缺口 = max(预测销量 × 1.35 + 安全库存 - 可覆盖库存, 0)；
6. 按缺口从高到低输出备货建议，列出 SKU、供应商、仓库、当前库存、预测需求、缺口、交期风险和建议动作；
7. 生成《夏日生活节备货方案》草稿，并标记“待采购人员审核”。
```

注意：销售预测工具当前支持按 SKU 查询；活动日期和备货系数作为本次演示的业务上下文，由 Dumate 在分析中应用，不要声称 MCP 已执行促销计算。

## 5. 供应商沟通归档与跟进

先粘贴下面这段模拟沟通文本：

```text
2026-08-11 08:42，采购林珊通过邮件联系禾木食品供应链李琦：低糖燕麦饼 240g 原计划交期已延期，请供应商今天 18:00 前确认新的到货日期，并说明是否影响夏日生活节备货。
```

然后执行：

```text
请将上面的供应商沟通内容转成 Dumate 内的结构化归档和跟进任务：
1. 识别供应商、联系人、SKU、沟通类型、时间、关键节点、承诺事项和截止时间；
2. 用 get_supplier_info 校验供应商 SUP-002；
3. 用 get_supplier_communications 查询 SUP-002 既有记录，判断是否与 COM-001 重复；
4. 用 get_purchase_orders 和 get_order_status 校验关联订单及延期状态；
5. 生成沟通归档草稿和一条待办任务；
6. 输出识别事实、重复判断、待确认字段和跟进任务。不要调用不存在的 upload_communication 工具，也不要声称已经写入 CRM。
```

## 6. 补货分析报告

```text
请生成《供应链补货分析报告》演示稿，数据范围为全部 SKU：
1. 查询全部库存和销售预测；
2. 查询全部未完成采购订单及延期订单；
3. 按安全库存缺口、预测缺口、订单交期风险计算补货优先级；
4. 输出三张表：安全库存缺口、促销期补货优先级、滞销/需人工复核清单；
5. 每行关联 SKU、仓库、供应商、证据字段和建议动作；
6. 报告开头注明演示数据和生成时间，结尾注明“建议待采购人员审核”；
7. 把报告保存为 Dumate 内的可复用模板，不写回 MCP。
```

## 7. 创建看板

```text
请基于前面已确认的数据和报告，在 Dumate 内创建一个“供应链协同演示看板”：
1. 顶部展示数据标签、数据生成时间和连接状态；
2. 第一块展示库存总览：总 SKU、低库存 SKU、安全库存达标情况；
3. 第二块展示订单追踪：未完成订单、延期订单、缺货订单；
4. 第三块展示促销备货：夏日生活节参与 SKU、预测缺口和优先级；
5. 第四块展示供应商协同：待回复沟通数、延期供应商和待办任务；
6. 每个指标必须能回溯到具体 MCP 工具和记录；
7. 页面只读，所有卡片标注“演示数据”，不要添加虚构客户成效。
```

## 8. 定时周报与收尾演示

```text
请创建一个供应链周报演示任务，但先不要启用发送：
1. 任务名称：供应链周报（演示）；
2. 时区：Asia/Shanghai；
3. 频率：每周一 09:00；
4. 内容：库存缺口、促销备货优先级、延期/缺货订单、供应商沟通待办、本周行动计划；
5. 输出格式：Markdown；
6. 先生成一次预览，等待我确认接收人、渠道和启用时间；
7. 明确说明 MCP 只提供原始数据，任务由 Dumate 调度。
```

最后使用：

```text
请把本次演示整理成一页《供应链协同案例演示总结》：
数据接入 → 风险识别 → 促销备货 → 沟通跟进 → 报告看板 → 定时任务。
列出每一步使用的 MCP 工具、Dumate 生成的业务对象、人工确认点和当前未实现能力。所有内容标注为演示数据。
```

## 演示时的关键口径

- MCP 是上游事实数据源，不是供应链业务应用本身。
- 补货建议、预警、看板、报告和定时推送全部由 Dumate 负责。
- 当前 MCP 是只读的，不能上传沟通、创建采购单或修改订单状态。
- 不要展示低于安全库存数量为“系统已触发预警”，应说“Dumate 根据库存事实计算出预警”。
- 不要展示补货数量为“系统已下单”，应说“Dumate 生成待审核建议”。
